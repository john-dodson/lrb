<%= form_for(@caterer_date) do |f| %>
  <% if @caterer_date.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@caterer_date.errors.count, "error") %> prohibited this caterer_date from being saved:</h2>

      <ul>
      <% @caterer_date.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :caterer_id %><br>
    <%= f.number_field :caterer_id %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :service_id %><br>
    <%= f.number_field :service_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
