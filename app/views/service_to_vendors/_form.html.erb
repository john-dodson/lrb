<%= form_for(@service_to_vendor) do |f| %>
  <% if @service_to_vendor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@service_to_vendor.errors.count, "error") %> prohibited this service_to_vendor from being saved:</h2>

      <ul>
      <% @service_to_vendor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :service_id %><br>
    <%= f.number_field :service_id %>
  </div>
  <div class="field">
    <%= f.label :vendor_id %><br>
    <%= f.number_field :vendor_id %>
  </div>
  <div class="field">
    <%= f.label :vendor_type_id %><br>
    <%= f.number_field :vendor_type_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
