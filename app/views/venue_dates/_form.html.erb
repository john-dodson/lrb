<%= form_for(@venue_date) do |f| %>
  <% if @venue_date.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@venue_date.errors.count, "error") %> prohibited this venue_date from being saved:</h2>

      <ul>
      <% @venue_date.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :venue_id %><br>
    <%= collection_select(:venue_date, :venue_id, Venue.all, :id, :name) %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :service_id %><br>
    <%= collection_select(:venue_date, :service_id, Service.all, :id, :service_type) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
  
<% end %>
