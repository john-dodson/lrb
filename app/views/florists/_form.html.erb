<%= form_for(@florist) do |f| %>
  <% if @florist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@florist.errors.count, "error") %> prohibited this florist from being saved:</h2>

      <ul>
      <% @florist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
