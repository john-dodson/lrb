<%= form_for(@florist_date) do |f| %>
  <% if @florist_date.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@florist_date.errors.count, "error") %> prohibited this florist_date from being saved:</h2>

      <ul>
      <% @florist_date.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :florist_id %><br>
    <%= f.number_field :florist_id %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :service_id %><br>
    <%= f.number_field :service_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
