<%= form_for(@vendor_type) do |f| %>
  <% if @vendor_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vendor_type.errors.count, "error") %> prohibited this vendor_type from being saved:</h2>

      <ul>
      <% @vendor_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :vendor_type %><br>
    <%= f.text_field :vendor_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
